<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Sortie{% endblock %}</title>
        <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <header>

    <nav class = "navbar navbar-expand-lg navbar-light bg-light">
        <div>
        <a class="navbar-brand" href="/"> <font style = "vertical-align: inherit;"> <font style = "vertical-align: inherit;"> Sortie </font> </font> </a>
        <button class = "navbar-toggler" type = "button" data-toggle = "collapse" data-target = "# navbarColor03" aria-controls = "navbarColor03" aria-Expand = "false" aria-label = "Basculer la navigation ">
            <span class = "navbar-toggler-icon"> </span>

        </button>
        </div>

        <div class = "collapse navbar-collapse" id = "navbarColor03">
            <ul class = "navbar-nav mr-auto">
                <li class = "nav-item active">
                    <a class="nav-link" href="/sortie"> <font style = "vertical-align: inherit;"> <font style = "vertical-align: inherit;"> Sortie prévu</font> </font> <span class = "sr-only"> <font style = "vertical-align: inherit;"> <font style = "vertical-align: inherit;"> (actuel) </font> </font> </span > </a>
                </li>
                {% if app.user %}
                <li class = "nav-item">
                    <a class="nav-link" href="{{ path('sortie_create') }}"> <font style = "vertical-align: inherit;"> <font style = "vertical-align: inherit;"> Créer une sortie </font> </font> </a>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                <li class = "nav-item">
                   <a class="nav-link" href="{{ path('app_signup') }}"> <font style = "vertical-align: inherit;"> <font style = "vertical-align: inherit;"> Inscription </font> </font> </a>
                </li>
                {% endif %}
                {% endif %}



            </ul>


        </div>

        {% if app.user %}
            <div>
                Bienvenue
                <a href="{{ path('app_editProfil') }}" class="logo" style="color:blue">
                    {{ app.user.pseudo}}
                </a>

                <a href="{{ path('app_logout') }}" class="logo">Déconnexion</a>
            </div>
        {% else %}
            <div>
                <a href="{{ path('app_login') }}" class="logo">
                    Connexion
                </a>
            </div>
        {% endif %}



        </nav>
    </header>
    <div class="container">
        {% block body %}{% endblock %}
    </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
