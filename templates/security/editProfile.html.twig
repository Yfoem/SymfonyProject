{% extends 'base.html.twig' %}

{% block body %}
    {% if error is defined and error != ""%}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <strong>Erreur !</strong> {{ error }}
        </div>
    {% endif %}
    {% if success is defined and success != ""%}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
            <strong>Succès !</strong> {{ success }}
        </div>
    {% endif %}

    <div>
        <p></p>
        <p></p>
        <a href="{{ path('app_editPassword') }}" class="logo">

            <p><span style="font-weight: bold;color: blue;text-decoration: underline; "> Modifier mot de passe</span></p>
            <p></p>
        </a>
    </div>
    <div>
    </div>
    {{ form_start(editProfilFormType) }}
    {{ form_widget(editProfilFormType) }}
    <button class="btn btn-primary" type="submit">S'enregistrer</button>
    {{ form_end(editProfilFormType) }}


{% endblock %}

{% block title %} Profile {% endblock %}